# dependencies
/node_modules
/.pnp
.pnp.*

# env / secrets — NEVER commit
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# next.js build output
/.next/
/out/

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store
*.pem
.vscode/
.idea/

# Senso context seeds — generated dev artifacts, not source
senso-context/
fetch_pharmacies.js

# Seed scripts — local-only data ingestion (not app code, breaks Vercel build
# via dotenv import + gets typechecked by tsconfig). Keep locally, never commit.
/scripts/

# Scratch/probe files (same risk — ad-hoc test scripts that get typechecked by
# tsconfig and break the Vercel build). Keep locally, never commit.
/scratch/

# agent scaffolding (not relevant to the project)
AGENTS.md
CLAUDE.md
.zcode/
.agents/
.claude/
.cursor/
